openai>=1.0.0
pyyaml>=6.0
jinja2>=3.0
ragas>=0.2.0
datasets>=2.18.0
langchain-openai>=0.1.0
# chromadb>=0.4.0  # 의존성 충돌로 일시 제거
langchain>=0.1.0
langchain-community>=0.0.1
sentence-transformers>=2.2.0
tiktoken>=0.5.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# PDF 처리
pypdf>=3.0.0
pdfplumber>=0.10.0

# 이미지 처리 및 OCR
Pillow>=10.0.0
pdf2image>=1.16.0

# OCR 엔진 (우선순위 순)
easyocr>=1.7.0           # 권장: 고성능, 한글 지원
paddlepaddle>=2.5.0      # PaddleOCR 기반 (Python 3.12 호환)
paddleocr>=2.7.0         # 최고 성능 OCR
# pytesseract>=0.3.10    # 폴백: 시스템에 tesseract-ocr 설치 필요

# DeepSeek-OCR (선택적 - GPU 권장)
# transformers>=4.45.0   # DeepSeek-OCR 지원
# vllm>=0.8.5            # DeepSeek-OCR 추론 엔진
# flash-attn>=2.6.0      # 고속 어텐션 (선택)

# 추가 문서 로더
unstructured>=0.10.0
