# Docker 빌드용 경량 의존성
# dataset-pipeline CLI 기능에 필요한 핵심 패키지만 포함

# Core LLM
openai>=1.0.0

# Configuration
pyyaml>=6.0
jinja2>=3.0
python-dotenv>=1.0.0

# LangChain (문서 처리)
langchain>=0.1.0
langchain-community>=0.0.1

# Embeddings
sentence-transformers>=2.2.0

# Utilities
tiktoken>=0.5.0
tqdm>=4.65.0

# CLI
click>=8.0.0

# Testing
pytest>=7.0.0
